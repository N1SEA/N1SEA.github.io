/** @type {import('next-intl').NextIntlConfig} */
module.exports = {
  locales: ['en', 'fr'], // замени на нужные языки
  defaultLocale: 'en',
  // Если перевод хранится в папке messages
  messagesDirectory: './messages'
}
